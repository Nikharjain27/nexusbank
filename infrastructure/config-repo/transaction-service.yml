nexusbank:
  service:
    name: transaction-service
    version: 1.0.0

info:
  application:
    name: NEXUSBANK Transaction Service
    description: Financial transaction processing service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/